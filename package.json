{
  "name": "trashury",
  "private": true,
  "workspaces": [
    "shared",
    "backend"
  ],
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "dev:api": "npm run dev --workspace backend",
    "smoke": "bash scripts/smoke/alur.sh && node scripts/smoke/serentak.mjs && node scripts/smoke/balapan.mjs"
  }
}
